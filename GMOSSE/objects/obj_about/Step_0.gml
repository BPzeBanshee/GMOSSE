//input check
draw_y -= 0.5;
obj_title.y -= 0.5;
if (global.button1 = 0 && ready = 0) ready = 1;
if (global.button1 = 1 && ready = 1) || draw_y <= e
    {
    ready = 0;
    scr_playsnd(snd_click,0);
    obj_title.y = 75;
    instance_destroy();
    }

